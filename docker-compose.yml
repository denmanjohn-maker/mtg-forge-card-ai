version: "3.9"

services:
  # ─── MongoDB ────────────────────────────────────────────────────────────────
  mongodb:
    image: mongo:7
    container_name: mtg_mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: mtgforge
    volumes:
      - mongodb_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ─── Qdrant Vector Store ─────────────────────────────────────────────────────
  qdrant:
    image: qdrant/qdrant:latest
    container_name: mtg_qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"   # HTTP API
      - "6334:6334"   # gRPC API
    volumes:
      - qdrant_data:/qdrant/storage

  # ─── .NET 10 Backend ─────────────────────────────────────────────────────────
  mtgforge:
    build:
      context: ./MtgForgeAi
      dockerfile: Dockerfile
    container_name: mtg_forge_api
    restart: unless-stopped
    ports:
      - "5001:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - MongoDB__ConnectionString=mongodb://admin:password@mongodb:27017
      - MongoDB__DatabaseName=mtgforge
      - Qdrant__Host=qdrant
      - Qdrant__Port=6334
      # Ollama runs natively on the Mac host, not in Docker
      # (Apple Silicon Metal acceleration doesn't work inside Docker)
      - Ollama__BaseUrl=http://host.docker.internal:11434
      - Ollama__Model=llama3.1:8b
      # OpenTelemetry — telemetry sent to the collector, which fans out to Tempo / Prometheus
      - OTEL__Endpoint=http://otel-collector:4317
    depends_on:
      mongodb:
        condition: service_healthy
      qdrant:
        condition: service_started
      otel-collector:
        condition: service_started
    extra_hosts:
      - "host.docker.internal:host-gateway"

  # ─── OpenTelemetry Collector ──────────────────────────────────────────────────
  # Central telemetry hub: receives OTLP from the .NET service (gRPC) and from
  # the frontend browser app (HTTP with CORS), then fans out to Tempo and
  # Prometheus.  Expose 4317/4318 on the host so the frontend can reach it.
  otel-collector:
    image: otel/opentelemetry-collector-contrib:latest
    container_name: mtg_otel_collector
    restart: unless-stopped
    ports:
      - "4317:4317"   # OTLP gRPC  — used by the .NET service
      - "4318:4318"   # OTLP HTTP  — used by the frontend (browser, CORS-enabled)
      - "8889:8889"   # Prometheus scrape endpoint for OTLP-forwarded metrics
      - "13133:13133" # Health check
    volumes:
      - ./otel-collector-config.yaml:/etc/otelcol-contrib/config.yaml:ro
    depends_on:
      - tempo

  # ─── Prometheus ───────────────────────────────────────────────────────────────
  prometheus:
    image: prom/prometheus:latest
    container_name: mtg_prometheus
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.retention.time=7d'
    depends_on:
      - mtgforge
      - otel-collector

  # ─── Grafana Tempo (distributed tracing) ─────────────────────────────────────
  # Tempo no longer needs OTLP ports exposed to the host — all external telemetry
  # goes through the collector, which forwards traces internally to Tempo.
  tempo:
    image: grafana/tempo:latest
    container_name: mtg_tempo
    restart: unless-stopped
    ports:
      - "3200:3200"   # Tempo API / query (used by Grafana)
    volumes:
      - ./tempo.yaml:/etc/tempo.yaml:ro
      - tempo_data:/var/tempo
    command: ["-config.file=/etc/tempo.yaml"]

  # ─── Grafana (dashboards for Prometheus + Tempo + Loki) ──────────────────────
  grafana:
    image: grafana/grafana:latest
    container_name: mtg_grafana
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer
      - GF_FEATURE_TOGGLES_ENABLE=traceqlEditor traceToMetrics
      # Set GF_LOKI_URL to your Loki endpoint if using Grafana Cloud or a remote Loki
      - GF_LOKI_URL=${GF_LOKI_URL:-http://localhost:3100}
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - grafana_data:/var/lib/grafana
    depends_on:
      - prometheus
      - tempo

volumes:
  mongodb_data:
  qdrant_data:
  prometheus_data:
  tempo_data:
  grafana_data:
